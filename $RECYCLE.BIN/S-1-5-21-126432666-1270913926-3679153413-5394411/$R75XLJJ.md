## Pipeline failing for deployDispatcher true but missing its productIDs PackageIDs

In case pipeline failure in build stage for missing dispatcher productIDs/PackageIDs, rerun tenant deployment with adding dispatcher productIDs/packageIDs in customer input.
​​
