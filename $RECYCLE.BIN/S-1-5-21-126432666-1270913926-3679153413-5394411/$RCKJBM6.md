### Check for the Destroy Action in Tenant Repo

1. Go to the tenant repository.
2. Navigate to the file `customer-information/tenant.yml`.
3. Check if the variable named `stage` is set to `destroy`.
