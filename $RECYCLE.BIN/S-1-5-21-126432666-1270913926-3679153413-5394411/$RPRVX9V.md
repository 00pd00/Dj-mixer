## Manually unzip kits on windows

If for some reason kits extraction from zip fails (it can happen if pipeline encounters EBS(size related issue). in such cases manually extract the kits zip from EFS to D:\kits