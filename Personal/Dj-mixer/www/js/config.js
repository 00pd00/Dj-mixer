// Global app configuration/constants
const CONFIG = {
  DEFAULT_VOLUME: 80,
  PLAYER_WIDTH: 100,
  PLAYER_HEIGHT: 70,

  // Paste your YouTube Data API v3 key here to enable in-app search.
  // Get one free at https://console.cloud.google.com/apis/credentials
  // (enable "YouTube Data API v3" on the project first).
  // Leave blank to disable search - the rest of the app works fine without it.
  YOUTUBE_API_KEY: '',
};
